<INSTRUCTIONS>
Pythonファイルを変更した場合は、Ruffを適用すること。
インデントにはスペース2個を使用すること。

```text
ruff check --fix broccoli_mcp tests
ruff format broccoli_mcp tests
```

`.clang-format`は`.h`、`.cpp`ファイルにのみ適用すること。
</INSTRUCTIONS>
